services:
  - type: web
    name: sentinel-core
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 60 --workers 2
    envVars:
      - key: GROQ_API_KEY
        sync: false
